# Shared credential for the manual OTA endpoint. Generate one locally, for example:
#   openssl rand -hex 32
VIEWE_OTA_TOKEN=replace-with-a-random-shared-token

# Optional board ID used by release creation.
# VIEWE_OTA_BOARD=meter
